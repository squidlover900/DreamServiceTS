# DreamService
## Documentation
### ContainerObject ->
ContainerObject.Data = {
  Private = false,
  CFrame = CFrame.new(),
}
ContainerObject.Map
### :CreateContainer(Dream : Folder) -> ContainerObject
