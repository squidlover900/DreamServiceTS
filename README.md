# DreamService
## Documentation
### ContainerObject ->
```lua
ContainerObject.Data = {
  Private = false,
  CFrame = CFrame.new(),
}
ContainerObject.Map
```

### :CreateContainer(Dream : Folder) -> ContainerObject
